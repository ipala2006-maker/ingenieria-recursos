name: Build Android app

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "android-app/**"
      - "scripts/bandeja.js"
      - "scripts/pomodoro.js"
      - "scripts/workspace.js"
      - "scripts/account.js"
      - ".github/workflows/android-app.yml"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Prepare stable Android signature
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          test -n "$ANDROID_KEYSTORE_BASE64"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "${RUNNER_TEMP}/estudiemos-release.jks"

      - name: Build signed installable APK
        working-directory: android-app
        env:
          ANDROID_KEYSTORE_PATH: ${{ runner.temp }}/estudiemos-release.jks
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: gradle :app:assembleRelease --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Estudiemos-Android
          path: android-app/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error

      - name: Publish permanent Android download
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cp android-app/app/build/outputs/apk/release/app-release.apk "${RUNNER_TEMP}/Estudiemos-Android.apk"
          VERSION_NAME=$(sed -n 's/.*versionName "\([^"]*\)".*/\1/p' android-app/app/build.gradle)
          if gh release view android-latest >/dev/null 2>&1; then
            gh release upload android-latest "${RUNNER_TEMP}/Estudiemos-Android.apk" --clobber
            gh release edit android-latest \
              --title "Estudiemos para Android" \
              --notes "Versión ${VERSION_NAME}. Este enlace siempre descarga la versión Android más reciente."
          else
            gh release create android-latest \
              "${RUNNER_TEMP}/Estudiemos-Android.apk" \
              --target "${GITHUB_SHA}" \
              --title "Estudiemos para Android" \
              --notes "Versión ${VERSION_NAME}. Este enlace siempre descarga la versión Android más reciente."
          fi
